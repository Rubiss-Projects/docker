services:
  valheim:
    image: lloesche/valheim-server:latest@sha256:bf11bb9fefc58e29cbfd646ba2acffbe368018caf33cf72748764afe92fd268c
    container_name: valheim
    cap_add:
      - sys_nice
    volumes:
      - ./config:/config
      - ./data:/opt/valheim
    ports:
      - "2456-2457:2456-2457/udp"
      - "9001:9001/tcp"
    env_file:
      - path: .env
      - path: .env.secret
        required: false
    restart: unless-stopped
    stop_grace_period: 2m
    labels:
      - homepage.group=Gaming
      - homepage.name=Valheim
      - homepage.icon=/icons/valheim.png
      - homepage.description=Valheim Server
      - homepage.widget.type=gamedig
      - homepage.widget.serverType=valheim
      - homepage.widget.url=udp://valheim:2457
      - homepage.widget.fields=["status", "currentPlayers", "maxPlayers", "ping"]
    networks:
      - proxynet

networks:
  proxynet:
    external: true
    name: proxynet
