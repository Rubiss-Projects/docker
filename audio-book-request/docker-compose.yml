services:
  web:
    image: markbeep/audiobookrequest:1
    container_name: audio-book-request
    restart: unless-stopped
    ports:
      - '8000:8000'
    volumes:
      - ./config:/config
    healthcheck:
      test: ["CMD", "wget", "--quiet", "--tries=1", "-O", "/dev/null", "http://localhost:8000/api/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 15s
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
    labels:
      - homepage.group=Media Management
      - homepage.name=Audiobook Request
      - homepage.icon=audiobookshelf.png
      - homepage.href=https://audiobookrequest.benlawson.dev/
      - homepage.siteMonitor=http://audio-book-request:8000/api/health
      - homepage.description=Audiobook request manager
    networks:
      - proxynet

networks:
  proxynet:
    name: proxynet
